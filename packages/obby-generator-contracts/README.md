# `@obby/obby-generator-contracts`

Phase G0 schema, public contract types, canonical hash helpers, typed failures, and fail-closed
graph validators. JSON Schema Draft 2020-12 owns structural validation; TypeScript declarations
and a schema fingerprint are generated and drift-checked.

See [`docs/generator/contracts.md`](../../docs/generator/contracts.md).
